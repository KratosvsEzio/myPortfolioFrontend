import { DataToolTipDirective } from './data-tool-tip.directive';

describe('DataToolTipDirective', () => {
  it('should create an instance', () => {
    const directive = new DataToolTipDirective();
    expect(directive).toBeTruthy();
  });
});
