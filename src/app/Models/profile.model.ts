// tslint:disable-next-line: class-name
export interface profileData {
  name: string;
  image: string;
  address: string;
  email: string;
  specialization: string;
  recentDegree: string;
  githubURL: string;
  cvURL: string;
}
